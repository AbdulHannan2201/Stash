#ifndef UTILS_HPP
#define UTILS_HPP

#include<bits/stdc++.h>

using namespace std;

void typeOut(const string& type, int time = 30);
string prntRed(const string& red);
string prntGreen(const string& Green);
void clrscr();
#endif